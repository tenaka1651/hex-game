{
  "name": "my-hex-engine-game",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "@hex-engine/2d": "^0.9.1"
  },
  "devDependencies": {
    "hex-engine-scripts": "^0.9.1",
    "typescript": "^4.9.3"
  },
  "scripts": {
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && hex-engine-scripts dev",
    "dev": "hex-engine-scripts dev",
    "build": "hex-engine-scripts build",
    "test": "hex-engine-scripts test"
  }
}
